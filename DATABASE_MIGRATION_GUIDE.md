# Подробное руководство: Применение миграций базы данных

## Способ 1: Supabase CLI (Рекомендуется) ⚡

### Шаг 1: Установка Supabase CLI

Если CLI еще не установлен:

```bash
# macOS
brew install supabase

# Windows (PowerShell)
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase

# Или скачать бинарник напрямую
# https://github.com/supabase/cli/releases
```

### Шаг 2: Логин в Supabase

```bash
# Откроет браузер для авторизации
supabase login
```

### Шаг 3: Инициализация проекта

```bash
# В корне проекта (где есть папка supabase/)
supabase init

# Если уже инициализирован, пропустите этот шаг
```

### Шаг 4: Применение миграций

```bash
# Применить все миграции из supabase/migrations/
supabase db push

# Или указать конкретный файл
supabase db push --file supabase/migrations/20250221_add_profession_tables.sql
```

**Вывод должен быть примерно таким:**
```
Connecting to remote database...
Applying migration 20250221_add_profession_tables.sql...
Finished supabase db push.
```

---

## Способ 2: Supabase Dashboard (Веб-интерфейс) 🌐

### Шаг 1: Открыть SQL Editor

1. Перейдите в [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. В левом меню нажмите **"SQL Editor"**
4. Нажмите **"New query"**

### Шаг 2: Применить миграцию структуры

1. Откройте файл `supabase/migrations/20250221_add_profession_tables.sql`
2. Скопируйте ВЕСЬ текст из файла
3. Вставьте в редактор SQL
4. Нажмите **"Run"** (или Ctrl+Enter)

**Проверка:**
```sql
-- Выполните этот запрос для проверки
SELECT * FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('profession_forecasts', 'profession_salaries');
```

Должны появиться 2 таблицы.

### Шаг 3: Импорт данных о прогнозах

1. Откройте файл `scripts/data-collection/output/profession-forecasts-2026.sql`
2. Скопируйте текст (может быть большим - ~800 строк)
3. Вставьте в новый SQL Editor
4. Нажмите **"Run"**

**Проверка:**
```sql
SELECT COUNT(*) FROM profession_forecasts;
-- Должно вернуть: 83
```

### Шаг 4: Импорт данных о зарплатах

1. Откройте файл `scripts/data-collection/output/profession-salaries-2026.sql`
2. Скопируйте текст
3. Вставьте в новый SQL Editor
4. Нажмите **"Run"**

**Проверка:**
```sql
SELECT COUNT(*) FROM profession_salaries;
-- Должно вернуть: 70
```

---

## Способ 3: Через psql (для продвинутых) 🖥️

### Шаг 1: Получить строку подключения

1. В Supabase Dashboard перейдите в **Settings → Database**
2. Найдите раздел **"Connection string"**
3. Выберите **"URI"** формат
4. Скопируйте строку (замените `[YOUR-PASSWORD]` на реальный пароль)

### Шаг 2: Применить миграции через командную строку

```bash
# Перейти в папку проекта
cd путь/к/gradpath-analytics

# Применить миграцию структуры
psql "postgresql://postgres:[PASSWORD]@db.[PROJECT-ID].supabase.co:5432/postgres" \
  -f supabase/migrations/20250221_add_profession_tables.sql

# Импортировать прогнозы
psql "postgresql://postgres:[PASSWORD]@db.[PROJECT-ID].supabase.co:5432/postgres" \
  -f scripts/data-collection/output/profession-forecasts-2026.sql

# Импортировать зарплаты  
psql "postgresql://postgres:[PASSWORD]@db.[PROJECT-ID].supabase.co:5432/postgres" \
  -f scripts/data-collection/output/profession-salaries-2026.sql
```

---

## Проверка установки ✅

После применения всех миграций выполните проверку:

```sql
-- 1. Проверить таблицы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- Должны быть:
-- profession_forecasts
-- profession_salaries

-- 2. Проверить view
SELECT viewname 
FROM pg_views 
WHERE schemaname = 'public';

-- Должен быть:
-- profession_analytics

-- 3. Проверить количество данных
SELECT 'forecasts' as table_name, COUNT(*) as count FROM profession_forecasts
UNION ALL
SELECT 'salaries', COUNT(*) FROM profession_salaries;

-- Должно быть:
-- forecasts: 83
-- salaries: 70

-- 4. Проверить view profession_analytics
SELECT * FROM profession_analytics LIMIT 5;

-- Должны отображаться профессии с зарплатами и рейтингами
```

---

## Устранение неполадок 🔧

### Проблема 1: "Table already exists"

**Решение:** Миграция уже применена, пропустите этот шаг или используйте:

```sql
-- Для удаления и пересоздания (ОСТОРОЖНО!)
DROP TABLE IF EXISTS profession_forecasts CASCADE;
DROP TABLE IF EXISTS profession_salaries CASCADE;
DROP VIEW IF EXISTS profession_analytics CASCADE;
```

### Проблема 2: "Permission denied"

**Решение:** Убедитесь, что используете роль с правами администратора (postgres).

### Проблема 3: "Unique violation" при импорте данных

**Решение:** Данные уже есть, используйте ON CONFLICT:

```sql
-- В файлах SQL уже есть ON CONFLICT, но если нужно обновить:
INSERT INTO profession_forecasts (...)
VALUES (...)
ON CONFLICT (profession_name, forecast_year) 
DO UPDATE SET
  demand_level = EXCLUDED.demand_level,
  updated_at = now();
```

### Проблема 4: Ошибка при применении через CLI

**Проверьте:**
1. Правильно ли указан project-id в `supabase/config.toml`
2. Есть ли доступ к интернету
3. Выполнен ли `supabase login`

```bash
# Проверить статус
supabase status

# Перелогиниться
supabase logout
supabase login
```

### Проблема 5: Не видно таблиц в Dashboard

**Решение:**
1. Обновите страницу (F5)
2. Проверьте, что выбрана правильная схема "public"
3. В Table Editor выберите **"public"** в dropdown схем

---

## Автоматизация (опционально) 🤖

### Скрипт для быстрого применения всех миграций

Создайте файл `apply-migrations.sh`:

```bash
#!/bin/bash

echo "🚀 Применение миграций базы данных..."

# Проверка наличия Supabase CLI
if ! command -v supabase &> /dev/null; then
    echo "❌ Supabase CLI не установлен. Установите: https://supabase.com/docs/guides/cli"
    exit 1
fi

# Применение миграции структуры
echo "📊 Создание таблиц..."
supabase db push --file supabase/migrations/20250221_add_profession_tables.sql

# Импорт данных
echo "📥 Импорт прогнозов..."
supabase db push --file scripts/data-collection/output/profession-forecasts-2026.sql

echo "📥 Импорт зарплат..."
supabase db push --file scripts/data-collection/output/profession-salaries-2026.sql

echo "✅ Готово!"
echo ""
echo "Проверка данных:"
supabase db query "SELECT 'forecasts' as table, COUNT(*) as count FROM profession_forecasts UNION ALL SELECT 'salaries', COUNT(*) FROM profession_salaries;"
```

Сделайте исполняемым:
```bash
chmod +x apply-migrations.sh
./apply-migrations.sh
```

---

## Быстрая проверка через приложение 🚀

После применения миграций:

1. Запустите приложение:
```bash
npm run dev
```

2. Откройте в браузере:
```
http://localhost:5173/applicants
```

3. Прокрутите до секции **"Востребованные профессии 2026"**

4. Должны отображаться:
   - Статистика (83 профессии, средняя зарплата)
   - Топ-5 профессий
   - Фильтры по спросу и категориям
   - Карточки с данными

Если данные не загружаются:
- Откройте DevTools (F12)
- Вкладка Console - проверьте ошибки
- Вкладка Network - проверьте запросы к Supabase

---

## Видео-подсказка 🎥

Если предпочитаете визуальное руководство:

1. Supabase Dashboard Method:
   - Открыть SQL Editor
   - Вставить SQL из файлов
   - Нажать Run

2. Supabase CLI Method:
   - Открыть терминал
   - Выполнить: `supabase db push`
   - Подтвердить

---

## Часто задаваемые вопросы ❓

**Q: Нужно ли каждый раз применять миграции при обновлении кода?**
A: Нет, только один раз. Если структура БД не менялась, повторно применять не нужно.

**Q: Безопасно ли запускать миграции на продакшене?**
A: Да, но сначала протестируйте на dev/стейджинге. Миграции используют IF NOT EXISTS.

**Q: Что делать, если миграция упала на полпути?**
A: Проверьте, какие таблицы создались, удалите их и запустите снова:
```sql
DROP TABLE IF EXISTS profession_forecasts CASCADE;
DROP TABLE IF EXISTS profession_salaries CASCADE;
```

**Q: Можно ли обновить данные без удаления таблиц?**
A: Да, просто перезапустите SQL-файлы с данными. ON CONFLICT обновит существующие записи.

---

## Следующие шаги

После успешного применения миграций:
1. ✅ Проверьте данные в Dashboard
2. ✅ Запустите приложение
3. ✅ Проверьте страницу /applicants
4. 🔄 Регулярно обновляйте данные о зарплатах через парсер

Если возникли проблемы - обратитесь к разделу "Устранение неполадок" выше или напишите в поддержку.
